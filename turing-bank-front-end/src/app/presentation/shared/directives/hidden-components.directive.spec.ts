import { HiddenComponentsDirective } from './hidden-components.directive';

describe('HiddenComponentsDirective', () => {
  it('should create an instance', () => {
    // const directive = new HiddenComponentsDirective();
    // expect(directive).toBeTruthy();
  });
});
